import 'package:flutter/material.dart';

// COLORS
Color darkPrimaryColor = const Color(0xff2A2A2A);
Color appBarColor = Colors.transparent;

// Padding
EdgeInsets horizontalPadding = const EdgeInsets.symmetric(horizontal: 30);

// Variables
double navBarIconSize = 26;

// Icons
Icon backArrowButton = Icon(
  Icons.arrow_back_ios_new_rounded,
  color: darkPrimaryColor,
);
